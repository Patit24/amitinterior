{
  "name": "amit-interior",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:pages": "GITHUB_PAGES=true vite build && node scripts/prepare-github-pages.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  },
  "dependencies": {
    "firebase": "^12.15.0",
    "gsap": "^3.15.0"
  }
}
